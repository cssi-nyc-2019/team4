/*let user = name;

function greeting()
	"Welcome " +userName
	

let name = function(person_name, greet) {
  let text = Welcome + ',' + name;
  let person_name = text
  text = "Enter your name here";
  person_name = prompt(text); stop 

  console.log(text);
  var greet = greet_name(person_name, greet);
};
greet_name()

*/
